<!DOCTYPE html>
<html>
  <head>
    <title>Undercast</title>
    <meta name="viewport" content="width=device-width, user-scalable=no" />
    <meta name="turbolinks-cache-control" content="no-cache">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="text-grey-darkest">
    <div class="container mx-auto mt-4 pb-24 px-4">
      <div class="mb-10 py-2 border-b-2 text-center">
        <%= link_to 'Shows', shows_path, class: "no-underline uppercase text-grey hover:text-grey-dark" %>
      </div>

      <%= yield %>
    </div>

    <div id="now-playing" data-turbolinks-permanent>
      <%= content_for :now_playing %>
    </div>
  </body>
</html>
